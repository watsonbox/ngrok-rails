require 'spec_helper'

describe Ngrok::Rails do
  it 'has a version number' do
    expect(Ngrok::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
